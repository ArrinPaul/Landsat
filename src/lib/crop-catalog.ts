// Shared crop list for the AI prediction tools (/predict and /crop-advisor), which both let
// the user pick a generic crop for yield/advice predictions — distinct from the regional
// CROP_OPTIONS in src/lib/farm-options.ts used for a farmer's own farm profile.
export const GENERIC_CROP_OPTIONS = ["Corn", "Wheat", "Rice", "Soybeans", "Cotton", "Potatoes", "Tomatoes", "Barley", "Sorghum"];
